'use client';

import React from 'react';
import { DataProviderDashboard as EnhancedDataProviderDashboard } from './data-provider/DataProviderDashboard';

export const DataProviderDashboard: React.FC = () => {
  return <EnhancedDataProviderDashboard />;
};

export default DataProviderDashboard;